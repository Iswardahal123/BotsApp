## 2025-05-15 - [Standardized Help and Feedback]
**Learning:** Consolidating user feedback strings in `lib/db.ts` and using dynamic prefix extraction ensures consistent UX across different bot configurations.
**Action:** Always use placeholders for prefixes and command counts in help strings and extract the active prefix dynamically from `config.ts`.
